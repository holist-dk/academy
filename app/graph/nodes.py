"""
Department node functions.

Each department becomes one node function here (or is imported from
its own department folder - see docs/departments.md - and registered
here). A node reads the shared state, does one department's job, and
returns a state update. Nodes never call each other directly (ADR-008
Law 1). Left empty until Phase 11 (department scaffolding) exists.
"""