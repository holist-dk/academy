"""
Graph construction - the Control Shell (ADR-008).

This file will assemble the LangGraph StateGraph: register all
department nodes, wire routing, set the entry point, and compile
the graph. Left empty until real departments exist to wire together
(Phase 11). See scripts/langgraph_toy/toy_graph.py for a minimal
working example of the pattern this will follow.
"""